import React from "react";

export default function Hero() {
  return (
    <section className="hero-section">
      <h2 className="hero-title">Accessible AI Speech-to-Text</h2>
      <p className="hero-subtitle">
        Turn spoken audio into clear, accessible plain language text — built for deaf and hard-of-hearing users.
      </p>
    </section>
  );
}
